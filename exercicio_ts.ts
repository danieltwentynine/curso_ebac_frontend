function multiplicacao(x: number, y: number): number {
    return x * y;
}
function saudacao(nome: string): string {
    return `Olá ${nome}, isso está muito fácil!`;
}
console.log(multiplicacao(2, 9));
console.log(saudacao("EBAC"));
//Autor: Daniel M Cardoso